# lib/

Non-React utilities.

- `sfx.ts` — audio playback helpers. Completion-sound juice lives here.
- `storage.ts` — localStorage read/write with schema versioning (if we don't fold this into the Zustand `persist` config).
